#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int T;
    cin >> T;

    while (T--) {
        int N, K;
        cin >> N >> K;

        int answer = N + (N - 1) / (K - 1);
        cout << answer << '\n';
    }

    return 0;
}
